stages:
  - test
  - deployement

job_test:
  image: php:7.4
  before_script:
    - apt-get update -y
    - composer install
    - composer update

  script:
    - phpunit phpunit_myapp.xml tests

job_deployment:
  script:
    - echo "Déploiement en cours..."
